/**
 * SnapOverlay — Visual indicator shown when a window is being dragged near screen edges.
 * Phase 1 placeholder: The actual snap zone detection happens in WindowFrame via onDrag.
 * This component will render semi-transparent rectangles when snap is active.
 * For now, it's a no-op placeholder that will be connected in Phase 2.
 */
export function SnapOverlay() {
  // TODO: Connect to window drag state to show snap zone previews
  return null
}
